#VITE_API_URL=https://ronet.onrender.com:443
VITE_API_URL=http://localhost:8080
VITE_TENANT_ID=00000000-0000-0000-0000-000000000001
# Serve routeurApi.list() from fixtures instead of /api/routers (see routeurApi.ts).
VITE_MOCK_ROUTEURS=true
# Serve the "client simple" interfaces (HomePage, ForfaitsAchatPage, PaiementPage,
# MesSouscriptionsPage, MesAccesPage) from fixtures instead of the real API — see
# forfaitApi.mock.ts / souscriptionApi.mock.ts / tokenApi.mock.ts / paymentApi.mock.ts /
# paymentMethodApi.mock.ts. Toggle to false to go back to the real backend at any time.
VITE_MOCK_CLIENT_DATA=true